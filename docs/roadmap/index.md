# Roadmap — Features in Planning

This section contains implementation plans for features that have been fully researched and architecturally specified but are not yet implemented. Each page captures the design intent, API surface, error model, and key decisions so that implementation can start without re-doing the research.

These are **living design documents** — they reflect the current best thinking at the time of writing. API surface may change slightly during implementation as new constraints are discovered.

---

## Planned features

| Feature | Package | Status | Summary |
|---------|---------|--------|---------|
| [Stream — FlatMap](stream-flatmap.md) | `stream` | Awaiting use case | FlatMap sub-stream variant (semaphore pool, unordered merge; main IO driver now covered by `ports.IOPort` + 1→N adapters) — deferred until a concrete driver appears. Also records will-not-implement decisions (`RetryWithBackoff`). GroupBy/Switch routing SHIPPED in `stream/route.go`; CombineLatest5+ resolved via nested composition (stream guide) |
| [AMQP 0.9.1 Adapter](amqp-adapter.md) | `adapters/amqp` | Design complete | PUB/SUB + Request/Reply over RabbitMQ — exchange/queue topology, Ack/Nack, `ReplyTo`/`CorrelationId` RPC, structured errors, observer integration |
| [TCP Adapter](tcp-adapter.md) | `adapters/tcp` | Design complete | Request/Reply + streaming over raw TCP — pluggable `FramedConn` framing, built-in length-prefix framer, stdlib-only, no CGO |
| [WebSocket — Deferred](websocket-deferred.md) | `adapters/websocket` | Deferred | Phases 1+2 SHIPPED (server adapters + `DuplexPort` + `SocketPattern`; client dial adapters with auto-reconnect + gap SocketErrors + session generations; chi variants via swapHandler delegation; `RegisterSocket` AsyncAPI channel). Remaining: `ConnectionObserver` extension, dynamic subprotocol negotiation. Will-not-implement: MQTT-over-WS, universal StreamPattern, outbound queueing across gaps |
| [Redis — Phase 2: Pub/Sub](redis-pubsub.md) | `adapters/redis` | Design draft | `SubscribeAdapter` (SourcePort) + `PublishAdapter` (SinkPort) via existing `EventPattern`; new narrow `PubSubCommands` interface + fake; at-most-once semantics documented loudly (offline subscribers LOSE messages — closer to ZeroMQ than MQTT); `PSUBSCRIBE` glob derivation from `{var}` templates; reuses `RecordSubscribe`/`RecordPublish` (no new stats extension). Redis Streams = Phase 3 candidate; still deferred from Phase 1: DelAdapter, CachePattern spec rendering. Per-var key codecs SHIPPED — see [Redis Cache Adapter](../features/redis.md#per-key-variable-codecs--portscachekeyparam) |
| [Webhook Adapter](webhook-adapter.md) | `adapters/webhook` | Design complete — Phase 0 prerequisite identified | `ReceiveAdapter` (SourcePort) + `DeliverAdapter` (SinkPort) — HMAC-SHA256 signature verification/signing over raw payload bytes, the one mechanism generic `nethttp`/`chi` can't do today (the declarative `.HandleMW`/`.ClientMW` security mechanisms run after body decode). Body-preserving verify middleware wraps a small, additive `nethttp.IngestAdapterOptions.Middleware` hook (a Phase 0 prerequisite, since `nethttp.Handler` was removed by middleware-workflow-simplification); `handle.EncodeRequest` gives raw bytes to sign before `nethttp.CallWithHandle`. Reuses `RESTPattern` (OpenAPI) + optional `EventPattern` (AsyncAPI) unchanged; retry-with-backoff mirrors `adapters/websocket`'s reconnect loop; no new stats extension (`SecurityObserver`/`Observer` reused) |
| [Fuzz & Benchmark Testing Infrastructure](fuzz-benchmark-testing.md) | `validate`, `codex`, `format` | Design complete | Internal quality initiative, no new API. Fuzz targets for every hand-rolled string/byte parser (9 `validate` regex constraints, `codex.HexColor`'s byte-level hex parser, JSON/TOML format-boundary decode) — zero fuzz targets exist today. Benchmarks for hot paths (`Struct` encode/decode, `SliceOf`/`StringMap`, `Refine` chains, `format.JSON` round trip) with `b.ReportAllocs()` — zero benchmarks exist today. New CI `fuzz` job (`-fuzztime=30s` per target); `benchstat`-comparable baseline for future regression checks |
| [Dynamic Port Rebinding (Hot-Swap Adapters)](dynamic-port-rebinding.md) | `ports` | Design draft | Live adapter cutover on running ports without process restart. Phase 1 scopes explicit `BindRef`/`Rebind`/`Unbind` on single-binding ports (`IOPort`, `DuplexPort`, `LatestPort`) with generation-based lifecycle tracking, typed `slog.LogValuer` errors, and observer reporting on all transition paths. **Cross-referenced with [Declarative Middleware](declarative-middleware.md)'s "L11"**: this doc's motivating "credential rollover" scenario is ALREADY solved for `ports` by that doc's per-call middleware attachment (zero mechanism from this doc needed there) — `Rebind` here is scoped to swapping the underlying TRANSPORT ADAPTER itself (broker failover, endpoint rotation, phased migration), a different concern. REST/events/reqreply's immutable `RouteHandle`/`ChannelHandle` middleware hot-swap remains an acknowledged gap in both docs, not yet designed. |
| [Declarative Middleware](declarative-middleware.md) | `middleware`, `stats`, `ports` | **SUPERSEDED for REST (request/response AND SSE) by [Middleware Workflow Simplification](../design/d-0001-rest-middleware-workflow-simplification.md) and [D-0003 — Codec-Declared Middlewares](../design/d-0003-codec-declared-middlewares.md), BOTH IMPLEMENTED. SUPERSEDED for events pub/sub, ALSO by D-0003** (this doc's original `events.WithMiddleware`/`mqtt5.RequireScopes[T]`-style proposal never shipped this way; `api/events` security shipped via [D-0002](../design/d-0002-pubsub-workflow-simplification.md), its codec-backed middleware declaration via D-0003, mirroring REST exactly). `api/reqreply`'s own dispatch/`Attach` workflow was tracked separately in [D-0004 — ReqReply Workflow Simplification](../design/d-0004-reqreply-workflow-simplification.md), now fully IMPLEMENTED, not here — but reqreply's MIDDLEWARE specifically (the `.Use()`/`.HandleMW()`/`.ClientMW()` declare/implement split REST/events both have) is its own distinct gap, now tracked in [ReqReply Middleware](reqreply-middleware.md). **Doc trimmed to its remaining scope: MCP (`api/mcp`/`mcpgo`, observability-only) and `ports` (`File`/`Cache`/`SQL`/`Dir`, decorator-shaped) — both still Phase 2+ design rationale, not yet implemented.** | Core thesis (still valid for the remaining scope): routes/channels/ports should reduce to pure input/output, with cross-cutting concerns attached externally via ONE shared, composable mechanism — REST/events already proved this via d-0001/d-0003; `ports.File`/`Cache`/`SQL`/`Dir` and MCP remain the open test of whether the SAME mechanism (or D-0003's own `Declaration[In,Out]`-based alternative, per its "Feasibility" analysis) genuinely generalizes beyond REST/events. `ports.File` Read AND Write decorator shapes sketched and proven (see the doc's "Ports get the same treatment" section); MCP's observability-only resolution (collecting every attached `stats.Observer`-typed `Fn` via the existing `stats.NewFanout`, avoiding a silent-drop bug) is fully speced. `middleware.ContextField[V]` (the codec-typed, ctx-carried value bus) has ALREADY SHIPPED (`middleware/context_field.go`, alongside d-0001) and is available for MCP/ports to reuse without further design work. Remaining known limitations tracked: L5 (MCP Resources/Prompts coverage), L11 (cross-reference with [Dynamic Port Rebinding](dynamic-port-rebinding.md)), L12 (MCP observer fan-out), L14 (Layer 3/forge — spun out to [Forge Pipeline Middleware](forge-pipeline-middleware.md)). **Driver clarified this round**: the `ports.File`/`Cache`/`SQL`/`Dir` work here is driven by the library's own UX North Star (declarative/simple/consistent workflow), explicitly NOT the same mechanism as [Protocol-Native Features](protocol-native-features.md)'s Attach-time `Capability` (ports have no `Attach` step) — the two docs solve structurally different problems and are not blocked on each other. |
| [ReqReply Middleware](reqreply-middleware.md) | `api/reqreply`, `adapters/mqtt5`, `adapters/zeromq`, `render/asyncapi/v3` | **Phase 0 + Phase 0b SHIPPED (mqtt5 AND zeromq); Phase 1/1b SHIPPED (mqtt5); zeromq's own Fn-shape now ALSO SHIPPED (see ZeroMQ Security Mechanism)** | The ONE remaining structural gap between `api/reqreply` and its REST sibling now that [D-0004](../design/d-0004-reqreply-workflow-simplification.md)'s `Server`/`Client`/`Attach` rework shipped: `reqreply.Route` has no `.Use()`/`.HandleMW()`/`.ClientMW()` declare/implement split. Mirrors `rest.Route`'s mechanism BYTE-FOR-BYTE (identical signatures/semantics, reqreply's request/response shape being REST-shaped, not events'-pub/sub-shaped) — `RouteHandle.Implementations`/`ClientImplementations` fields, mqtt5-only Phase 1 (mqtt(v3) permanently out of scope per protocol limitation; zeromq's OWN security Fn-shape, once a distinct deferred gap, has SINCE SHIPPED too, see [ZeroMQ Security Mechanism](zeromq-security.md)). All decisions LOCKED IN: coverage-check runs adapter-Serve-time (REST's exact precedent); `reqreply.WithSecurityScheme` is REPLACED by `.Use(middleware.SecurityScheme(...))` (full REST parity, old type kept as deprecated alias); the new paired security Fn adopts REST's scope-grant model (`(map[string][]string, error)` + `middleware.CheckScopes`). **Phase 1b**: closes the headers/cookies-as-middleware parity gap for MQTT5's User Properties (`mqtt5.UserPropertyParam` confirmed structurally identical to `middleware.HeaderParamSpec`) — `FromUserPropertyParam`/`FromResponseUserPropertyParam` bridge functions for BOTH request and reply/response direction, gated on a confirmed real prerequisite: `render/asyncapi/v3` has zero message-header rendering today (confirmed spec-compliant against the real AsyncAPI 3.0 spec). Also plans extending `examples/reqreply-api` with a middleware demo mirroring `examples/rest-api`'s exact structure. **Phase 0 + Phase 0b — SHIPPED for mqtt5 AND zeromq, outcome BETTER than originally planned**: found during this doc's final review pass that [D-0004](../design/d-0004-reqreply-workflow-simplification.md)'s "kept `Serve`/`Call`/`CallHandle`/`ServeRouter`/`CallDealer` permanently, mirrors REST's `ServeOne`/`CallWithHandle`" justification did NOT hold against the real code (REST's versions are zero-duplication thin wrappers; reqreply's old functions shared NO code with `AttachServer`/`AttachClient` — a genuine duplicate dispatch implementation, conflicting with "adapter stays a thin IO wrapper"). D-0004's bullet was REOPENED and reversed. Phase 0 (renamed from an earlier "opportunistic Phase 2") closed the ONE real capability gap (merge-fields, per-call format overrides, `ErrorPattern`, PLUS a newly added `reqreply.ClientCallOptions` type and 8 new `RouteHandle` format/merge-aware methods) — SHIPPED for mqtt5, verified via `go build`/`go vet`/`gofmt`/`go test -race`/`just check`/all examples, all clean, plus 6 new tests. Phase 0b THEN found, while implementing the planned new-named thin wrappers, that `reqreply.ServerTransport.Serve`/`ClientTransport.Call` are ALREADY single-route/single-call scoped — so `Serve`/`Call`/`CallHandle` were simply rewritten to DELEGATE to `serverTransport`/`clientTransport` directly: zero duplicate logic, EXACT SAME signatures, NO breaking change, NO deletion, NO `examples/reqreply-api` migration needed at all (Demo 2/3 needed zero code changes) — superseding the original "delete + rebuild + migrate" plan entirely. A real, additional gap (missing `stats.TraceObserver` span support in the reflection-based Attach dispatch) was found and closed along the way, via re-running the FULL pre-existing test suite (not just the new one) — exactly the discipline this doc's own "Delivery risk & staging" section calls for. **zeromq received the SAME Phase 0 + Phase 0b treatment in a follow-up pass, same session**: `serverTransport`/`routerServerTransport`/`clientTransport`/`dealerClientTransport` gained the same format-override/`ErrorPattern`/`TraceObserver` capability parity (merge-field DECODE support confirmed NOT APPLICABLE — zeromq's wire format carries no topic frame at all, routing is purely socket-based); `Serve`/`Call`/`CallHandle`/`ServeRouter`/`CallDealer` now delegate the SAME way, zero breaking change. One zeromq-specific regression found and fixed via the same full-test-suite-rerun discipline: a `BuildTopic` failure during observability-path derivation must be FATAL (matching the escape hatch), not silently ignored — an initial draft got this wrong, causing a test to hang indefinitely until caught. MQTT3 confirmed already fully out of scope (zero `reqreply` code exists in `adapters/mqtt` today) — nothing left to do there. zeromq's own SECURITY Fn-shape mechanism — once DESIGN-DECIDED (same in-payload `*Req` mechanism as pub/sub, no new wire-level work) — is now SHIPPED too (see [ZeroMQ Security Mechanism](zeromq-security.md)), closing what was the ONE genuinely open zeromq follow-up. **Critically reviewed**: "full parity" scope stays NARROWED to the primary declare/implement/bind/attach workflow (Phase 1 + 1b) — Phase 0/0b's capability-parity work was a PREREQUISITE for that claim to hold cleanly, not part of the parity claim itself. A "Delivery risk & staging" section stages verification independently per phase — Phase 0 and Phase 0b each fully verified alone, both clean, before Phase 1 starts. |
| [Middleware Workflow Simplification](../design/d-0001-rest-middleware-workflow-simplification.md) | `middleware`, `api/rest`, `adapters/nethttp`, `adapters/chi` | **GRADUATED to docs/design/** | Fully implemented and shipped, including old-door removal (`Handler`/`Register`/`SSEHandler`/`RegisterSSE` deleted from both `adapters/nethttp` and `adapters/chi`; `Serve`/`ServeOne`/`ServeSSE` are the sole server-side entry points) — moved to [Design Documents](../design/d-0001-rest-middleware-workflow-simplification.md) per this section's graduation bar (establishes a pattern followed by REST + the `ports.Pattern` binding layer). |
| [REST `Client.Call` — dual-mode dispatch for `GlobalSecurity` visibility](rest-client-call-global-security.md) | `api/rest`, `adapters/nethttp` | Design draft | Spun out of the reqreply/REST ergonomics-parity review: REST's `Client.Call` always derives a fresh, Builder-free `Route.ClientHandle()` and explicitly rejects an already-registered `*RouteHandle`, so `GlobalSecurity` (declared via `Server.AddGlobalSecurity`) can never reach REST's client-side credential check — `resolveClientSecurity`'s existing `GlobalSecurity` fallback is dead code in practice. Proposes backporting reqreply's confirmed dual-mode `Client.Call` (raw `Route` OR registered `*RouteHandle`, type-switched) to REST's `nethttp` client transport — purely additive, no breaking change to the raw-`Route` path. Open questions: exact type-check mechanism for the new `*RouteHandle` branch, whether `Client.Consume` (SSE) has the identical gap, and whether a throwaway spike should precede implementation. |
| [Events SecurityFunc/CredentialFunc Removal](events-security-func-removal.md) | `api/events`, `adapters/mqtt5`, `adapters/zeromq`, `adapters/mqtt` | Draft — decisions locked, ready for implementation planning | Aligns `api/events` with REST's D-0001 and reqreply's Phase 1 precedent: removes the permanently-coexisting imperative `SubscribeOptions.SecurityFunc`/`PublishOptions.CredentialFunc` escape hatch now that the declarative `SubscribeMW`/`PublishMW` mechanism (D-0002/D-0003) already covers every capability it provides, with a strictly richer Fn signature. `events.Channel.Handle()` already runs `CheckCoverage` unconditionally — no new wiring needed before removal. Decided: all 3 adapters (mqtt5, zeromq, mqtt v3) in one combined round; `examples/adapters-mqtt-security` (mqtt-v3-only, SecurityFunc-based, with its own documented no-op `SubscribeMW` placeholder) is DELETED and replaced by a new `examples/events-api` mini-project mirroring `rest-api`/`reqreply-api`'s multi-package layout, demonstrating the declarative security mechanism end-to-end across all 3 adapters. |
| [ZeroMQ Security Mechanism](zeromq-security.md) | `adapters/zeromq` | SHIPPED | Spun out of the pub/sub doc's escape-hatch review. `adapters/zeromq` pub/sub already has the in-payload `SecurityFunc`/`CredentialFunc` mechanism (shipped, D-0002). The optional out-of-band frame-based mechanism and connection-level/CURVE exposure are CLOSED — researched real-world CURVE/ZAP practice and confirmed `adapters/zeromq`'s deliberate library-agnostic design (zero concrete ZeroMQ dependency) makes connection-level config permanently the caller's own concern, mirroring TLS. **zeromq's reqreply Fn-shape is now SHIPPED**: `.Use()`/`HandleMW`/`ClientMW` wired into all 4 transports (REQ/REP + ROUTER/DEALER), 12 new tests, `examples/reqreply-api`'s Demo 9 demonstrates it alongside a cross-API OAuth2 scheme-sharing showcase (the SAME `middleware.SecurityScheme` value attached to a zeromq reqreply route AND a locally-declared REST route). |
| [ReqReply Codec-Declared Middleware](reqreply-codec-declared-middleware.md) | `api/reqreply`, `adapters/mqtt5`, `adapters/zeromq` | Design draft — 4 review rounds complete, 1 decision deferred (PropertyParam location), not yet implemented | Brings `api/reqreply` up to [D-0003](../design/d-0003-codec-declared-middlewares.md)'s parity level — a SECOND, additive, codec-backed `Middleware[In,Out]` mechanism (`Transform`/`ClientTransform`, bundled `WithReceive`/`WithSend` for route-agnostic `.Use()`) for non-security enrichment/derived-data concerns, entirely on top of the existing flat `.Use()`/`HandleMW`/`ClientMW` security mechanism ([ReqReply Middleware](reqreply-middleware.md)'s Phase 1/1b, unchanged). Not a deliberate D-0003 exclusion — purely chronological (D-0003 predates reqreply's own `Server`/`Client`/`Attach` rework and flat middleware split). API surface confirmed against `api/rest/transform.go`'s ACTUAL `Transform`/`ClientTransform` signatures (mirrors REST's request/reply-symmetric shape exactly). Vocabulary: events' topic-var axis PLUS a NEW, protocol-neutral **property** axis (`WithRequestProperty`/`WithResponseProperty`, via a new `PropertyParam`/`MergedPropertyParam[T]`/`NewPropertyParam[T,V]` triple mirroring `TopicParam`'s wrapper pattern) — resolved after confirming MQTT5 User Properties and AMQP message headers are the SAME cross-transport concept, not MQTT5-specific. Confirmed `api/events` has the IDENTICAL gap — tracked as an explicit, smaller companion follow-up (`WithSubscribeProperty`/`WithPublishProperty`), not implemented in this round. Naming (`reqreply.Middleware[In,Out]` vs `middleware.Middleware`) confirmed safe, zero collision. Transport-agnostic by design — zero adapter-specific work expected for mqtt5 vs zeromq (a required property on an adapter with no property mechanism naturally surfaces a validation error, no special-casing). One small remaining question: shared vs. per-package location for the new `PropertyParam` triple once the events companion is scoped. **Round 2**: re-verified against REST's ACTUAL implementation code (not just its signatures) and fixed 4 issues — `DecodeIn`/`EncodeOut`/etc. take/return SEPARATE topic/property maps, never combined (mirrors REST's real multi-map signatures); dispatch order stated explicitly (declared middleware runs AFTER the paired security Fn); a missing AsyncAPI spec-rendering integration added (unifies with Phase 1b's existing `Message.Headers` schema rendering); merge-field name conflict detection resolved — follows REST's stricter `ConflictingParamContributionError` precedent, a deliberate, accepted divergence from Phase 1b's laxer silent-dedupe behavior. **Round 3**: deep-compared the new `PropertyParam`/`MergedPropertyParam[T]`/`NewPropertyParam[T,V]` sketch against reqreply's OWN real, shipped `TopicParam`/`MergedTopicParam[Req]`/`NewTopicParam` and fixed 4 parity gaps — corrected `MergedPropertyParam[T]`'s struct shape (now a single embed of `codex.MergedParam[T]`, was a disconnected separate `Field`); added missing `WithDescription`, `WithCodec`, and internal `toParam`/`applyRoute` (routing into a new `rb.propertyParams` slice) methods, all mirroring `TopicParam`/`MergedTopicParam[Req]`'s real method set. **Round 4**: full end-to-end re-read found/fixed 2 bugs — a self-contradiction leftover (a later paragraph still said `DecodeIn` combines topic+property vars into one map, contradicting Round 2's fix) and matching ambiguous test-plan wording — plus resolved a new open decision (#6): which adapter `ErrorKind` wraps the new mechanism's own errors. `KindDecode`/`KindEncode` for merge-side failures; a NEW `KindMiddleware` value added to both `mqtt5.ErrorKind` and `zeromq.ErrorKind` for `MiddlewareError` (D2's fn-business-error fallback), distinguishing declared-middleware failures from real handler failures. One decision remains open: shared vs. duplicated `PropertyParam` location (deferred until the events companion is scoped). |
| [Common-Base + Per-Pattern-Derived Middleware Types](common-middleware-architecture.md) | `middleware`, `api/rest`, `api/reqreply`, `api/events`, future `ports` | SUPERSEDED by Codec-Backed Policy Declarations | Spun out of the pub/sub doc's critical-review finding that `middleware.Middleware` — a single flat struct — is imported directly by `api/rest`, `api/reqreply`, AND `api/events`, even though only REST has any use for its 5 param-contribution fields. Its original proposal (retrofitting `middleware.Middleware` into a `Common` base + per-pattern derived STRUCTS) would have been a breaking change to already-shipped code. Superseded by [D-0003 — Codec-Declared Middlewares](../design/d-0003-codec-declared-middlewares.md), which resolves the SAME finding non-breakingly via an additive `middleware.RouteMiddleware` marker interface + new per-pattern generic `Declaration[In,Out]`-backed types. |
| [Feature — sealed, per-adapter capability interfaces](protocol-native-features.md) | `api/events`, `api/rest`, `api/reqreply`, `api/mcp`, `ports`, every adapter | Idea only — no code written; core mechanism + D-0003 relationship + Handler Disposition + Observer integration all RESOLVED via 6 throwaway Go prototypes across multiple rounds | Originally spun out of the pub/sub doc's review of spec-adding custom middleware; grew after confirming THREE structural limits: `api/events/mqtt_qos.go` is MQTT-specific code inside the transport-agnostic package; MQTT5-only capabilities (User Properties, Shared Subscriptions) have no clean declaration surface; `events.NewChannel`'s flat `Topic string` cannot represent AMQP's exchange/routing-key/queue addressing model. An initial open `Feature`+string-ID `Provider.Supports` design was REJECTED — runtime-only mismatch rejection, violating a hard "no compile-time-safety compromise" requirement. **Confirmed mechanism: sealed, per-adapter `Capability` interfaces** (mirroring `ports.Pattern`'s technique, applied per-adapter), capabilities supplied at `Attach`/bind time, NOT baked into a channel/route's own declared type — a mismatch (e.g. mqtt-v3-only client receiving an mqtt5-only capability) is a **Go compile error**. Every surveyed capability (QoS, User Properties, AMQP addressing/ack-mode) confirmed to have no protocol-agnostic meaning — honest adapter-owned coupling, not a thin-adapter violation; Security remains the one genuinely cross-protocol case, unaffected, staying in the agnostic core. `Address` parameterization (`Channel[Addr,T]`) CONFIRMED compile-time-safe both directions, including the topic-var mechanism generalizing across MQTT/AMQP address shapes. AMQP's ack+persistence CONFIRMED to need 3 capabilities split by ROLE (`Persistent`/`PublisherConfirms` publish-only, `AckMode` subscribe-only), with role-split `PublishAttach`/`SubscribeAttach` confirmed compile-time-safe both directions. **§8 adds Handler Disposition** — a DISTINCT, ctx-signal mechanism (mirroring `nethttp.WithResponseHeaders`) letting a handler signal ack/nack/requeue outcomes, confirmed reusable across `events` AND `reqreply` unchanged; explicitly NOT yet extended to `ports` (different, non-message-dispatch architecture). **§3's relationship to [D-0003 — Codec-Declared Middlewares](../design/d-0003-codec-declared-middlewares.md) is now RESOLVED** (not merely reopened) via a confirmed 4-stage lifecycle model (declare → capability-declare → handler-attach → adapter-attach): both `Middleware[In,Out]` and `Capability` are stage-2, spec-contributing declarations, sharing a lifecycle stage without merging into one Go type; a decoupled `CapabilitySpec`+drift-check design was confirmed as the winning mechanism after two rejected alternatives (a threaded type parameter broke ergonomics; type-erased storage broke compile-time safety). Answers [MQTT5 User Property Merge](mqtt5-user-property-merge.md)'s open registration-surface question. [D-0004 — ReqReply Workflow Simplification](../design/d-0004-reqreply-workflow-simplification.md)'s `Client`/`Server`/`Attach` rework has now SHIPPED, resolving the prerequisite for Response Topic/Correlation Data specifically. **Handler Disposition's Observer integration is now RESOLVED too**: a new, optional `stats.DispositionObserver` (mirroring `SecurityObserver`'s exact type-assertion pattern) reports the ack/nack/requeue outcome ADDITIVELY — `RecordSubscribe`'s existing `success bool` stays UNCHANGED (confirmed a "derive success from disposition" alternative would silently break its documented contract) — confirmed necessary via a concrete case: a handler with no processing error that explicitly discards a message would otherwise be observably indistinguishable from a normal success. **Migration blast radius is now RESOLVED too**: a repo-wide grep count (not estimate) found 272 real compiled Go call sites of `events.NewChannel[T](...)` (21 in examples, 141 in `api/events`'s own tests, 110 in adapter conformance tests) plus 2 godoc examples and 26 documentation-site snippets — 300 total references that `Channel[Addr,T]`'s signature change would touch, confirmed genuinely large, with an unverified hypothesis that most follow one mechanically-migratable shape. **A "## Test plan" section is now RESOLVED too**: added (mirroring D-0003's own section), covering compile-time positive/negative test pairs for sealed `Capability`/AMQP role-split rejection, `Address`/`NewChannelFromTopic` ergonomics, Handler Disposition's ctx-sink round trip and default-fallback, `DispositionObserver`'s additive behavior, and §3's 4-stage lifecycle spec-contribution/drift-check — explicitly deferring shared `Middleware[In,Out]` test cases to D-0003's own Test plan. **Discovery is now RESOLVED too**: confirmed via code search that this codebase has ZERO existing precedent for a runtime "list what's supported" API on any sealed mechanism (`ports.Pattern`, `rest.SecurityScheme` are both godoc/exported-symbol discoverable only) — `Capability` following the same IDE-autocomplete/godoc discovery path is consistent with precedent, not a novel gap; a dedicated `ListCapabilities()`-style API remains a future option if a concrete driver ever appears. Remaining open items: spec-rendering wiring details (deferred until a concrete adapter is picked to prototype the wire format against) and `ports.File`'s capability mechanism (§5.6/Review-7 — **clarified this round**: a driver now exists (the library's UX North Star), but it points at [Declarative Middleware](declarative-middleware.md)'s own decorator-shaped ports design, not this doc's Attach-time `Capability` mechanism, which ports structurally cannot host). |
| [SSE Resume & Retry Policy](sse-resume-and-retry-policy.md) | `adapters/nethttp`, `adapters/chi`, `api/rest` | Findings only — no proposal, no driver yet | Spun out from SSE Client Consumption's "Out of scope" (that roadmap doc's Phase 1 shipped and was removed after a `review-go-codex` pass confirmed zero gaps against its committed scope — see `docs/design/d-0001-rest-middleware-workflow-simplification.md`'s addendum for the design lineage) once investigation showed BOTH `Last-Event-ID` resume and pluggable retry policy are genuinely client+SERVER features, not client-only follow-ons — confirmed via code that the SSE writer never emits `id:`/`event:`/`retry:` protocol fields anywhere today (only bare `data:` lines), and `stream.BroadcastHub` has no past-event buffer for any replay mechanism. Sketches two INDEPENDENT sub-features: (A) `Last-Event-ID` resume — server assigns+writes per-event `id:`, buffers recent events, replays from an incoming `Last-Event-ID` header; client tracks+resends the last-seen id on every reconnect. (B) pluggable retry policy — TWO DISTINCT halves: a client-side `RetryPolicy` func for go-codex's own `Consume`/`CallSSEAdapter`, and a server-side `retry:` field emission that spec-compliant browser `EventSource` clients honor natively, independent of go-codex's client machinery entirely. Cross-references `webhook-adapter.md`'s identical-shaped `RetryPolicy` idea as a precedent, while flagging the semantics differ (SSE reconnect is naturally infinite; webhook delivery is finite/give-up-eventually) — explicitly does NOT assume one shared type serves both without investigation. Flags 2 further out-of-scope items: a persistent/durable cross-restart event buffer, and SSE's `event:` field for multiplexed event types (related, but separate features, not expanded here). |
| [MQTT5 User Property Merge](mqtt5-user-property-merge.md) | `adapters/mqtt5` | Idea only — no driver, independent of Declarative Middleware | `UserPropertyParam` is validate-only, unlike REST's headers/cookies or `ports.File`'s path params — no `MergedUserPropertyParam[T]`/`NewRequiredUserPropertyParam`/`NewOptionalUserPropertyParam` sibling exists to merge a User Property directly into the decoded message struct. Mirrors `rest.MergedHeaderParam`'s exact shape; plugs into the SAME `codex.DecodeVars` merge call site `adapters/mqtt5` already uses for topic vars. Surfaced while reviewing Declarative Middleware but has no sequencing dependency on it. |
| [Forge Pipeline Middleware](forge-pipeline-middleware.md) | `forge`, `stats` | Idea only — no driver, spun out from Declarative Middleware's "L14" finding | `forge.Registry`/pipeline functions (Layer 3) were never checked against the Declarative Middleware design. Forge already has its own parallel cross-cutting mechanism (`Registry.WithObserver`/`stats.PipelineObserver`, an explicit builder API by design) and no security/authorization concept at all. Three open directions carried forward unresolved: document `Registry.WithObserver` as already adequate; actually extend `middleware.Middleware` to forge via a decorator-shaped `Fn` (mirrors `ports.File`); or simply narrow Declarative Middleware's "every boundary" claim to Layer 2 only. No concrete driver identified yet — deferred to a future dedicated design pass. |
| [Typed State Machine Contracts & Runtime](typed-state-machine.md) | `state`, `stream`, `forge` | Design draft | Codec-validated state payload + typed transition graph + runtime reducer so state can progress with explicit, forward-safe rules. Phase 1 includes both contract surface and runtime execution helper (no external FSM dependency). |
| [Vector Store Adapter (RAG retrieval)](vector-store-adapter.md) | `adapters/vectorstore`, `adapters/openai` (embeddings) | Awaiting use case | Placeholder for RAG's retrieval half — a narrow `VectorStore` interface (mirrors `adapters/redis.Commands`) for native vector DBs (Qdrant/Weaviate/Pinecone/Redis-search), plus the `adapters/openai` embeddings endpoint the (now-shipped) LLM Integration feature deferred. Notes what already works with ZERO new code today: pgvector similarity search via the existing generic `adapters/sql.QueryEachAdapter`, and embedding vectors via `codex.SliceOf(codex.Float32())`. Not yet designed in detail — captured as a reminder pending a concrete driving use case. |
| [OpenAI Multimodal Content](openai-multimodal-content.md) | `api/llm`, `adapters/openai` | Awaiting use case | Placeholder for image/audio/video input support — today `adapters/openai`'s wire shape is text/JSON-only (`chatMessage.Content` is always a plain string), with no path to OpenAI's multimodal `content` array (`image_url`/`input_audio` parts). Sketches a new `llm.CallOpt` for attaching media alongside the typed request codec and the `chatMessage.Content` union-type change needed on the adapter side. Not yet designed in detail — captured as a reminder pending a concrete driving use case. |
| [`PartialFrom` — Diff Two Structs Into a Patch](partial-struct-diff.md) | `codex` | Idea only | Follow-on to the now-SHIPPED Partial/Patch Struct Codec (`codex.PartialField`/`PartialStruct`, see `docs/concepts/codec.md`'s matching subsection and `.github/instructions/go-codex.instructions.md`) — auto-derive a patch by diffing a base/previous `T` against an updated `T`, populating only the CHANGED fields. Not scoped, no use case yet; the open question is per-field equality without reflection (many real `F` types aren't `comparable` in Go's strict sense). |
| [`codex.OptionalMutable[T]`: A Mutable Cell That May Start Unset](optional-mutable.md) | `codex`, `stats` | Design draft — `Mutable[T]` now shipped, ready to implement | `Mutable[T]`'s revalidating, hot-reloadable `Set` + `Maybe[T]`'s "may start unset" lifecycle, combined into a standalone THIRD type (not a modification of either) — for an optional feature's rotating credential that might not exist until the feature is enabled. Reuses `Mutable[T]`'s `codex.ReloadObserver` unchanged; `Get()` never panics (returns zero value if never `Set`, mirroring `Maybe[T]`'s precedent, not `Immutable[T]`'s panic-before-first-`Set`). Was explicitly sequenced AFTER `Mutable[T]` ships — that dependency is now satisfied — promoted from a one-line flagged idea once the need surfaced twice. |
| [`Mutable[T]`/`Cacheable[T]` — Subscribe-to-Changes](mutable-native-integration.md) | `codex` | Idea only — no driver | Of the 3 ideas this doc originally captured, 2 are now RESOLVED with no code change: native Builder security wiring (`SecurityFunc`/`CredentialFunc` are already plain closures — a caller composes `Mutable[T]`/`Cacheable[T]` via `.Get()` with zero new API, proven in `examples/mutable-security-keys` and documented in `docs/concepts/codec.md`'s "Composing with adapters" subsection), and folding `adapters/nethttp.NewCachingCredentialFunc` onto a container (redirected to `ports.RefreshingCacheable[T]`, not `Mutable[T]` — revisit after that ships). Only a "subscribe to changes" push-notification mechanism (distinct from `ReloadObserver`/`InvalidateObserver`, which only cover observability) remains open, with no driver and a genuinely open design space (channel-sub vs. callback vs. `stream.Stream[T]`). |
| [`Maybe[Nullable[T]]` (Three-State Fields)](maybe-nullable-and-codec.md) | `codex` | Idea only | A three-state field (absent/present-null/present-value — the RFC 7396 JSON Merge Patch shape) via `Maybe[Nullable[T]]` — VERIFIED to already compose for free with zero new code (`MaybeField("x", codex.Nullable(inner), ...)` on a `Maybe[*T]` field genuinely distinguishes all three states today); what's left is documentation-only (a worked example + test). (This doc's other idea — a public `codex.Codec[Maybe[T]]` — has SHIPPED as `codex.MaybeCodec[T]`, see `docs/concepts/codec.md`'s "`Maybe[T]`: definitive presence tracking" subsection.) |
| [`Cacheable[T]`: Redis-Backed Sibling](cacheable-redis-backed.md) | `adapters/redis`, `codex`, `ports` | Design draft — `codex.Cacheable[T]` now shipped, ready to implement | Follow-on to the now-SHIPPED in-process `codex.Cacheable[T]` (see `docs/concepts/codec.md`'s `Cacheable[T]` subsection and `codex/cacheable.go`) — a `adapters/redis.Cacheable[T]` sibling with the same TTL/`Invalidate()` shape but a ctx-aware, fallible `Get(ctx) (T, bool, error)` backed by an actual Redis entry via `ports.Cache`, so a value's storage is pluggable (in-memory or Redis) without changing how callers read it. Also captures a "generic multi-key `Cacheable`" idea as a no-driver-yet note. |
| [`ports.RefreshingCacheable[T]`](refreshing-cacheable.md) | `ports`, `codex`, `stats` | Design complete — `codex.Cacheable[T]` now shipped, ready to implement | Auto-refresh wrapper COMPOSING `codex.Cacheable[T]` (not modifying it) — `GetOrRefresh(ctx) (T, error)` single-flights a caller-supplied `RefreshFunc[T]` when stale, mirroring `adapters/nethttp.NewCachingCredentialFunc`'s existing hand-rolled single-flight/TTL pattern but generalized beyond HTTP credentials. Stale-while-revalidate is the default failure policy (`WithFailHardOnRefreshError` opts into fail-hard). Its `Cacheable[T]` dependency (previous roadmap row) has now shipped; a forward-looking note flags it as a candidate to later compose either the in-memory or Redis-backed `Cacheable[T]` sibling interchangeably. |
| [Streaming Walk Adapter for Files & Directories](dir-walk-adapter.md) | `adapters/file` | Idea only | Follow-on to the now-SHIPPED Directory Listing Port (`ports.Dir`/`DirEntry`, see `docs/features/ports.md`) — sketches a `ports.SourceAdapter`-shaped adapter that WALKS/streams `ports.DirEntry` values (recursive tree enumeration and/or continuous change-watching) instead of `ports.Dir.List`'s one-shot slice. Not designed: the central open question is a real dependency/architecture fork — stdlib-only polling (extends `adapters/file.WatchAdapter`'s existing approach) vs. event-based OS-native notifications (would require the FIRST external dependency in `adapters/file`, most likely `fsnotify/fsnotify`) — plus one-shot walk vs. continuous watch scope, and whether modify/delete events (not just creations) are needed. |
| [OpenAPI/AsyncAPI Spec Endpoint](openapi-spec-endpoint.md) | `api/rest` (+ `api/events`) | Idea only | `rest.Server.OpenAPISpec()` builds a spec document in memory, but go-codex has no declarative way to SERVE it over HTTP — every example that wants this (`examples/rest-api`) hand-rolls a plain `http.HandlerFunc`/chi handler registered before `AttachMux`/`AttachRouter`. Captures the gap and open questions (new `rest.Route` variant vs. `Server`-level mechanism, JSON/YAML content negotiation, static-at-startup vs. dynamic, whether `events.Client`/`AsyncAPISpec()` wants the same symmetry) — not designed in detail, no API surface committed yet. |
| [Request/Event Correlation ID](request-correlation-id.md) | `stats` (+ every adapter) | Idea only | A first-class, cross-cutting correlation ID for logs/metrics/support, spun out of reviewing [D-0004 — ReqReply Workflow Simplification](../design/d-0004-reqreply-workflow-simplification.md)'s wire-level `corrID`. Confirmed via code: no such concept exists anywhere today — `stats.Observer`'s own methods (`RecordRequest`/`RecordSubscribe`/`RecordPublish`) take no `ctx` at all, so they structurally can't read one even if ctx-stored. Proposes `stats.WithRequestID`/`RequestIDFromContext`/`NewRequestID`, mirroring `WithObserver`/`ObserverFromContext` exactly. **Key design question resolved**: this ID must stay DELIBERATELY SEPARATE from `TraceObserver`'s distributed trace/span ID and from reqreply's own wire-level `corrID` — three distinct concepts, not one. The clean connection to tracing is already validated by `TraceObserver.StartSpan(ctx, ...)`'s EXISTING signature (it already receives `ctx`): if the request ID is set into ctx BEFORE `StartSpan` is called, a tracer implementation can read it back internally and attach it as a span attribute — zero interface change needed, mirroring OpenTelemetry's own Baggage/attribute practice. Whether `Observer` needs a NEW additive `RequestIDObserver` extension (mirroring `DispositionObserver`'s pattern) is flagged, not locked — needs a throwaway prototype. `ports.File`/`Cache`/`SQL`/`Dir` coverage is explicitly flagged open (same structural point as `protocol-native-features.md`'s Review-7), not force-included. |

### Deferred — not planned for immediate implementation

| Feature | Package | Why deferred |
|---------|---------|-------------|
| `zeromq.CallDealerStream` | `adapters/zeromq` | Requires adding correlation-ID frames (`[seq_bytes, payload]`) to DEALER framing AND matching changes to `ServeRouter` — protocol-level breaking change, not a standalone stream bridge addition. The sequential `CallStream` (REQ socket) covers most use cases. |

---

## How to read these plans

Each plan page covers:

- **Motivation** — why this feature belongs in go-codex
- **Scope decisions** — what is in and out of Phase 1
- **API surface** — exact type signatures, options structs, interface definitions
- **Structured errors** — every new error type with `slog.LogValuer` attributes
- **Observer integration** — which observer hooks fire and when
- **AsyncAPI / OpenAPI spec** — how the feature integrates with spec generation
- **Files to create** — concrete file list with responsibilities
- **Usage sketch** — end-to-end code example
